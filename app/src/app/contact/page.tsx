import StaticPage from "@/components/StaticPage";

export default function ContactPage() {
    return (
        <StaticPage
            title="Contact Us"
            description="Reach out to the Only Workshop team for support, partnerships, or host onboarding."
        />
    );
}
