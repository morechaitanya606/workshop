import StaticPage from "@/components/StaticPage";

export default function CancellationsPage() {
    return (
        <StaticPage
            title="Cancellation Options"
            description="Cancellation and refund terms depend on each workshop host policy."
        />
    );
}
